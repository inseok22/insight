# AGENTS.md

## 목적
이 저장소에서 **회원가입 신청 기능(공개 신청 + 관리자 승인)** 을 안정적으로 구현하기 위한 공통 작업 지침입니다.

## 프로젝트 맥락
- 구조: **TSlurmDesk + TSlurm Insight 연동 구조**
- 현재 완료 사항: **TSlurmDesk 로그인 페이지 하단 가입신청 버튼 구현 완료**
- 다음 구현 대상(이번 가이드의 중심):
  - TSlurm Insight의 **공개 신청 페이지**
  - TSlurm Insight의 **관리자 승인 기능**

## 핵심 원칙
1. **큰 변경 전 계획 제시 필수**
   - API/DB 스키마/화면 흐름에 영향이 큰 변경은 착수 전에 계획(범위, 단계, 영향)을 먼저 공유한다.
2. **Small Diff 우선**
   - 한 번에 큰 리팩터링을 피하고, 기능 단위로 작고 검증 가능한 변경을 만든다.
3. **기존 Desk 로그인 버튼 동작 불변**
   - 이미 구현된 Desk 로그인 화면 하단 가입신청 버튼의 기존 동작은 변경하지 않는다.
4. **OpenLDAP 자동 연동 제외**
   - 이번 범위에서는 LDAP 자동 프로비저닝/동기화를 구현하지 않는다.
   - **관리자 수동 등록 + 승인 처리**만 지원한다.
5. **비밀번호 평문 금지**
   - 비밀번호는 저장/로그/응답/화면 표시에서 평문을 금지한다.
   - 서버 측 해시 기반 처리(예: Argon2/Bcrypt 등) 원칙을 지킨다.

## 회원가입 신청 기능 구현 가이드

### 1) 도메인 상태(권장)
- 신청 상태를 최소 아래처럼 분리:
  - `PENDING` (신청 대기)
  - `APPROVED` (승인)
  - `REJECTED` (반려)
- 운영 추적을 위해 `requested_at`, `approved_at`, `approved_by` 같은 감사 필드를 우선 고려한다.

### 2) API/권한(권장)
- 공개 엔드포인트:
  - 가입 신청 생성 (`POST`)은 인증 없이 가능
- 관리자 엔드포인트:
  - 신청 목록 조회/상세 조회
  - 승인/반려 처리
- 승인/반려는 관리자 권한 검증을 필수로 둔다.

### 3) UI/UX(권장)
- 공개 신청 페이지:
  - 필수 입력값 검증(아이디/이메일/사유 등 실제 요구사항 기준)
  - 제출 성공/실패 피드백 명확화
- 관리자 화면:
  - 상태별 필터, 승인/반려 액션, 처리 결과 피드백

### 4) 보안/개인정보
- 비밀번호/민감정보 마스킹 및 최소 수집 원칙
- 서버/클라이언트 로그에 민감정보를 남기지 않는다.
- 반려/승인 사유 저장 시 표시 범위(관리자 전용 여부)를 명확히 한다.

## 작업 절차
1. 변경 범위가 크면 먼저 계획을 공유한다.
2. 기능 단위로 작은 diff를 만든다.
3. 변경 후 아래 점검을 수행하고 결과를 공유한다.

## 점검/검증 명령어
### Frontend (`/workspace/insight/frontend`)
- 의존성 설치: `npm ci`
- 린트: `npm run lint`
- 빌드: `npm run build`

### Backend (`/workspace/insight/backend`)
- 의존성 설치: `pip install -r requirements.txt`
- 서버 기동 확인: `uvicorn app.main:app --reload`
- (테스트가 추가된 경우) 테스트: `pytest`

## 커밋/리뷰 규칙
- 커밋 메시지는 변경 의도가 드러나게 작성한다.
- PR 본문에는 아래를 포함한다.
  - 변경 요약
  - 범위 외 항목(예: LDAP 자동 연동 제외)
  - 보안 고려사항(비밀번호 평문 금지 적용 여부)
  - 검증 내역(린트/빌드/테스트)
